#include "SkinPackageOperationService.h"

// This translation unit intentionally contains declarations only in the Task
// 7 RED checkpoint. FIFO ownership and shutdown behavior are implemented with
// the focused lifecycle tests in the next phase.
